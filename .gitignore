# dependencies
node_modules/

# secrets — never commit your real API key
.env
.env.local

# alert bot dedup state (local runtime data)
.alerts-state.json
*.state.json

# logs / OS noise
*.log
.DS_Store
